#Here are some commands you should now for git
1. gitinit: initializes a directory into a repository
2. gitstatus: give a overview of files added, modified, deleted, since last commit and more
3. gitadd: adds files to be commited
4. gitcommit: commits the changes made and asks for a message to denote the commit
5. gitpush: finishes the process by pushing the changes to the main branch of the repo